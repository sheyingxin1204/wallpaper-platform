# 壁纸平台实施计划

> 状态：项目骨架已建立，尚未接入任何云资源、真实素材来源或生产密钥。

## 1. 目标与边界

本项目是一个公开浏览和下载壁纸的网站。网站负责展示已发布素材；管理员负责上传、补全来源与授权信息、审核和下架；自动任务只负责从**明确允许采集和再发布**的来源获取候选素材。

第一阶段的唯一业务闭环是：

```text
受授权素材 → 上传或采集 → 图片处理 → 待审核 → 管理员发布 → 公开浏览/下载 → 可追溯地下架
```

以下内容不属于第一阶段：普通用户注册、用户投稿、收藏、推荐系统、AI 生图、相似图搜索、自动公开发布和商业化功能。

## 2. 架构原则

| 职责 | 第一阶段选型 | 约束 |
| --- | --- | --- |
| Web、后台、轻量 API | Next.js App Router | 仅处理短请求，不转发图片大文件，不执行长时任务。 |
| 部署运行时 | Cloudflare Workers + OpenNext | 部署前必须用 Workers 预览环境验证，不假设所有 Node.js 包均可运行。 |
| 业务数据 | TiDB Cloud + Drizzle ORM | 图片二进制永不写入数据库。 |
| 图片对象 | Cloudflare R2 | 只保存对象 Key、元数据和衍生版本关联。 |
| 图片处理 | Node.js + Sharp | 只在本地管理工具或 GitHub Actions 中执行，不放入 Worker。 |
| 自动采集 | Node.js + Playwright | 独立于 Next.js；默认关闭，按来源逐个启用。 |
| 身份鉴别 | Better Auth | 第一阶段仅单管理员，不开放注册。 |

存储模块必须只通过 `StorageService` 接口访问。业务代码不得直接依赖 R2 SDK，以便未来迁移至 OSS 或 MinIO。

## 3. 第一阶段功能范围

### 3.1 公开网站

- 首页推荐和最新壁纸瀑布流。
- 分类、标签、方向、分辨率和颜色筛选。
- 壁纸详情、全屏预览、上一张/下一张和原图下载。
- 深色优先设计、响应式移动端适配。
- SEO 基础能力：可读 URL、页面 metadata、Open Graph、sitemap、robots。
- 浏览量和下载量统计；统计接口需要基础限流和去重策略。

### 3.2 管理后台

- 管理员登录与会话保护。
- 创建草稿、直传原图、编辑元数据。
- 审核、发布、下架、删除。
- 分类、标签、来源与授权信息管理。
- 查看采集记录和失败原因。

### 3.3 明确不做

- 用户注册、收藏、下载历史。
- 自动发布素材。
- 未经人工确认的网页采集来源。
- 全站搜索引擎、个性化推荐、以图搜图。
- 完整数据看板、付费下载和广告系统。

## 4. 数据模型与状态机

### 4.1 核心实体

| 实体 | 用途 | 最小必需字段 |
| --- | --- | --- |
| `wallpapers` | 壁纸业务主记录 | 标题、描述、状态、方向、宽高、主色、发布时间。 |
| `wallpaper_assets` | 原图和派生图 | R2 Key、用途、格式、宽高、字节数、SHA256、感知哈希。 |
| `categories` / `tags` | 分类与筛选 | 名称、slug、排序、启用状态。 |
| `wallpaper_tags` | 多对多关系 | wallpaper ID、tag ID。 |
| `sources` | 素材来源 | 来源名称、原始页面 URL、作者、抓取时间。 |
| `licenses` | 授权证据 | 授权类型、链接、说明、确认时间。 |
| `crawl_tasks` / `crawl_records` | 自动任务可追溯性 | Provider、输入、执行时间、结果、错误信息。 |
| `users` | 管理员账户 | 身份、权限、禁用状态。 |

### 4.2 壁纸状态

```text
draft → pending_processing → pending_review → published → unlisted
  └──────────────────────────────────────────────→ rejected
published ───────────────────────────────────────→ unlisted
```

- `draft`：创建成功但上传/元数据尚未完整。
- `pending_processing`：原图已确认，等待生成预览图和哈希。
- `pending_review`：处理完成，等待管理员核验来源、授权与质量。
- `published`：唯一允许在公开页面、sitemap 和下载入口出现的状态。
- `unlisted`：已下架；公开链接返回 404 或说明页，不再推荐或下载。
- `rejected`：审核不通过；保留审计记录，按保留策略删除对象。

状态变化必须记录操作者、时间、原因和前后状态。任何采集任务只能创建 `pending_review` 或更早的状态，不能写入 `published`。

## 5. 图片上传、处理与发布

### 5.1 上传流程

```text
管理员 → 申请短时 R2 PUT 预签名 URL → 浏览器直传暂存对象
→ API HEAD 校验对象 → 创建/更新草稿 → 图片处理任务 → 待审核
```

约束：

1. 申请 URL 的接口必须验证管理员会话。
2. URL 只签发指定对象 Key 和 `PUT` 操作，设置短过期时间。
3. 校验允许的 MIME 类型、文件大小和像素上限；浏览器提交的元数据不可信。
4. R2 CORS 只允许正式站点和本地开发域名。
5. 处理完成前，原图位于私有暂存路径；不得以公开 URL 直接暴露。
6. 发布前确认缩略图、960、1920、原图下载版本均存在，并写入 `wallpaper_assets`。

### 5.2 对象 Key 规范

```text
wallpapers/{year}/{month}/{wallpaper-id}/original.webp
wallpapers/{year}/{month}/{wallpaper-id}/preview-1920.webp
wallpapers/{year}/{month}/{wallpaper-id}/preview-960.webp
wallpapers/{year}/{month}/{wallpaper-id}/thumbnail-480.webp
```

对象 Key 由服务端生成，不接受客户端传入的任意路径。删除壁纸时先下架，再异步清理派生对象与原图，并记录清理结果。

## 6. 采集器规范

采集器使用 Node.js + Playwright；Python 不作为本项目默认实现。原因是主项目、采集脚本、类型定义和依赖管理可以使用同一套 TypeScript/Node.js 工具链。若未来有明确的 Python 图像算法需求，可作为独立工具引入，不与网站运行时耦合。

每一个 Provider 上线前必须满足以下条件：

1. 人工确认网站条款、robots.txt、授权类型和是否允许再分发。
2. 来源被记录在 allowlist；禁止“任意 URL 输入即可抓取”。
3. 实现固定 User-Agent、并发上限、速率限制、指数退避和失败日志。
4. 保存原始页面 URL、作者、许可信息、抓取时间和 Provider 版本。
5. 先按来源 URL 和 SHA256 去重，再用感知哈希标记近似重复，最终由人工决定。
6. 新素材默认 `pending_review`；未经审核绝不公开。
7. 收到权利人通知时，能够按来源或对象 Key 快速下架并保留处理记录。

GitHub Actions 初期只允许 `workflow_dispatch` 手动触发。稳定后再增加错峰定时任务；任务要幂等、可重试、可断点续跑，并提供任务 ID。Actions 不能承担严格准点或永不间断的调度承诺。

## 7. 安全、隐私与运维要求

- 所有密钥只放 Cloudflare、GitHub Actions 或本机 `.env.local`；禁止提交仓库。
- R2 Access Key、TiDB 连接串、管理员密钥和 Sentry DSN 全部使用部署平台的 Secret 管理。
- 后台/API 全部做身份校验；上传、发布、下架和删除需要审计日志。
- 下载和统计接口进行基础 IP/会话限流，避免刷量和滥用。
- 用户输入（标题、标签、来源 URL）进行 Zod 校验、长度限制和输出转义。
- 生产环境设置安全响应头、Content Security Policy 和明确的 CORS 策略。
- 数据库迁移只通过 Drizzle migration 执行；生产迁移先备份并记录版本。
- 定义备份策略：数据库定期导出，R2 保留原图与衍生图的清单，可从数据库重建索引。

## 8. 实施阶段与验收标准

### 阶段 0：技术验证

交付：本地 Next.js 能运行；Cloudflare Workers 预览可运行；TiDB 可连接；R2 可私有读写。

验收：`pnpm lint`、`pnpm typecheck`、`pnpm build` 和 Workers 预览通过；密钥未出现在 Git 历史中。

### 阶段 1：数据与管理闭环

交付：Drizzle Schema、管理员登录、R2 预签名上传、草稿编辑、图片处理和审核状态机。

验收：管理员可上传一张授权测试图片；未经发布的对象无法从公开页面访问；每次状态变化有审计记录。

### 阶段 2：公开浏览体验

交付：首页瀑布流、筛选、详情、预览、下载和基础 SEO。

验收：移动端和桌面端可用；已下架壁纸不出现在列表、搜索和 sitemap；下载链接仅指向已发布资源。

### 阶段 3：部署与可观测性

交付：Cloudflare 部署、环境变量、预发布环境、基础日志与 Web Analytics。

验收：推送到主分支可产出可访问版本；失败可回滚；错误可定位到请求和版本。

### 阶段 4：受控自动采集

交付：一个经确认授权的 Provider、手动 Actions 工作流、去重、处理和后台审核记录。

验收：重复执行同一任务不会产生重复发布记录；采集失败可重试；所有候选素材都可追溯来源和授权。

## 9. 上线前检查清单

- [ ] 至少一个来源具有明确可再分发授权。
- [ ] 管理员后台、上传接口和发布接口均已鉴权。
- [ ] R2 Bucket 不暴露写权限，CORS 已限制。
- [ ] 公开图片使用独立访问域名和缓存策略；私有暂存对象不可访问。
- [ ] 生产数据库迁移已执行并记录。
- [ ] `/robots.txt`、sitemap、站点 metadata 和下架策略已验证。
- [ ] 错误监控、日志和备份路径已验证。
- [ ] GitHub Actions Secrets、Cloudflare Secrets 和本地环境文件均不在仓库中。

## 10. 尚待确认的产品决策

1. 初始素材来源清单及每个来源的再发布授权证据。
2. 壁纸原图是否允许匿名直接下载，还是只提供指定分辨率。
3. 首批分类、标签和分辨率筛选口径。
4. 管理员登录方式：单个邮箱验证码、GitHub OAuth 或密码登录。
5. 图片对外访问域名、缓存时长和版权/下架联系渠道。

